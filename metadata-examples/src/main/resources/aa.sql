/*
    ReturnRule = List;
    Metadata = ru.bellski.metadata.examples.domain.AddressSqlMetadata;

    Parameters = [
        String param1,
        String param3,
        String param2,
        Integer param1
    ]
*/

SELECT *
FROM a
WHERE b = ? OR ?  OR ? OR ?

